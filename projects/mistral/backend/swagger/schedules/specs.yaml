file: schedules
class: Schedules
schema:
  expose: true
mapping:
  schedules: "/schedules"
  schedule:  "/schedules/<schedule_id>"
  schedule-requests: "/schedules/<schedule_id>/requests"
labels:
  - schedule