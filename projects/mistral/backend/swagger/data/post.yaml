common:
  custom:
    authentication: true 

data:
  summary: Request for data extraction.
  parameters:
    - name: criteria
      in: body
      description: Criteria for data extraction.
      schema:
        $ref: "#/definitions/DataExtraction"
  responses:
    '202':
      description: Data extraction request queued

