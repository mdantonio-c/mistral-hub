"""Namespace package for the access-key integration area.

The files in this subtree exercise both the lifecycle of user access keys and the
validation endpoint that consumes them through HTTP Basic authentication.
"""